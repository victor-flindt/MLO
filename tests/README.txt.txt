Runnig unit test on the data is as simple as installing pytest and running

<pytest -v tests\> from the MLO file, this will run the test found in test_data.py

which will asses the data and find potential faulty datapoints. 